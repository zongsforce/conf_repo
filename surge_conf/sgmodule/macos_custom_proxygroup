#!name=macOS Custom Proxy Group
#!desc=macOS Custom Proxy Group
#!category=local

[Proxy Group]
Google路由 = subnet, default = Google节点, "TYPE:CELLULAR" = Google节点, "TYPE:WIFI" = Google节点, "TYPE:WIRED" = Google节点, hidden = false
